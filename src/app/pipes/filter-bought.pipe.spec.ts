import { FilterBoughtPipe } from './filter-bought.pipe';

describe('FilterBoughtPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBoughtPipe();
    expect(pipe).toBeTruthy();
  });
});
