import { FilterExcPipe } from './filter-exc.pipe';

describe('FilterExcPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExcPipe();
    expect(pipe).toBeTruthy();
  });
});
